mybatis-plus:
  type-aliases-package: com.example.demo.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/clinic?currentSchema=warehouse
    username: postgres
    password: zhang123
server:
  port: 8082